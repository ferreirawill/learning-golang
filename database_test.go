package main

import (

	"testing"

)

func TestUserDatabase(t *testing.T) {
	/*db,err := initDatabase()

	if err != nil {
		t.Errorf("Error em init db %v",err)
	}*/
/*
	p := models.Person{
		Name: "William",
		Document: "12339210",
		Age: 21,
	}
	p.Create(db)
	
	p = models.Person{
		Name: "Lhais",
		Document: "111111",
		Age: 21,
	}
	p.Create(db)
	
	p = models.Person{
		Name: "Valdete",
		Document: "111111",
		Age: 35,
	}
	p.Create(db)

	*/
	/*
	np := models.Person{Name: "Valdete"}

	newperson,_ := np.ReadByName(db)


	fmt.Println("Idade de newPerson: ",newperson.Age)
	list,_ := np.ReadAll(db)
	//fmt.Println(list)

	for _,person := range list{
		fmt.Printf("Nome: %s | Idade: %d\n",person.Name,person.Age)
	}

	if np.Age != 21{
		t.Errorf("Recebido: %d| Esperado: 21",np.Age)
	}*/
}
